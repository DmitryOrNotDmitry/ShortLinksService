server:
  port: 8085

spring:
  application:
    name: gateway

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true

services:
  linkcreate: http://localhost:8081
  redirect: http://localhost:8082