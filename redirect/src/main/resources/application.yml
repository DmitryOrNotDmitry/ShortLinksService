server:
  port: 8082

spring:
  application:
    name: link-redirect-service
  cloud:
    discovery:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health, info
