server:
  port: 8082

spring:
  application:
    name: redirecting
  cloud:
    discovery:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health, info

db:
  url: jdbc:postgresql://localhost:5432/links_db